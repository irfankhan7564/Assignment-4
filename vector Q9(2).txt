#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> v1 = {1, 2, 3, 4, 5};
    vector<int> v2 = {1, 2, 4, 5};

    int sum1 = 0, sum2 = 0;

    for (int i = 0; i < v1.size(); i++) {
    sum1 =sum1+  v1[i];
    }
  for (int i = 0; i < v2.size(); i++) {
    sum2 = sum2+ v2[i];
  }

    cout << "Missing element: " << sum1 - sum2;

    return 0;
}
